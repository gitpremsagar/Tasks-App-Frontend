const API_BASE_URL = "http://localhost:2000";

export const USERS_ENDPOINT = `${API_BASE_URL}/api/users`;

export const VERIFY_TOKEN_ENDPOINT = `${API_BASE_URL}/api/users/verify-token`;

export const PROJECTS_ENDPOINT = `${API_BASE_URL}/api/projects`;

export const TASKS_ENDPOINT = `${API_BASE_URL}/api/tasks`;
